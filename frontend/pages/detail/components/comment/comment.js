import { myRequest } from "../../../../utils/sendUtil";
import { showErrorMessage, showTextMessage } from "../../../../utils/feedback";

Component({
    /**
     * 组件的属性列表
     */
    properties: {
        conmentList: Array,
    },

    /**
     * 组件的初始数据
     */
    data: {
    },

    /**
     * 组件的方法列表
     */
    methods: {

    },

    lifetimes: {
        ready: function () {
        },
    },
})
